import { Filters } from './Filters.jsx'

// eslint-disable-next-line react/prop-types
export const Header = () => {
  return (
    <header>
      <h1>React Shop</h1>
      <Filters />
    </header>
  )
}
